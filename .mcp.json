{
  "mcpServers": {
    "trilium": {
      "type": "http",
      "url": "${TRILIUM_MCP_URL:-http://localhost:8081/mcp}",
      "headers": {
        "Authorization": "${TRILIUM_ETAPI_TOKEN}"
      }
    }
  }
}
